
<h1><%= @cocktail.name %></h1>

<% @cocktail.doses.each do |d| %>
  <li><%= d.description %> <%= d.ingredient.name %>  <%= link_to 'delete a dose',  dose_path(d), method: :delete %></li>
<% end %>


<%= link_to 'back to the list', cocktails_path, class: 'btn btn-secondary' %>

<%= link_to cocktail_path(@cocktail), method: :delete, data: {confirm: "Are you sure?"}, class: 'btn btn-danger' do %> 
  Delete

  <%= link_to 'create a new dose', new_cocktail_dose_path(@cocktail), class: 'btn btn-warning' %>

<% end %> 